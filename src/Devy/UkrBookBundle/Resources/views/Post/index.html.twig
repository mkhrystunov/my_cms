{% set active = 'posts' %}
{% extends 'DevyUkrBookBundle::administrator.layout.html.twig' %}

{% block title %}
  Posts
{% endblock %}

{% block body -%}
  <table class="records_list table">
    <caption><h3>Posts</h3></caption>
    <thead>
    <tr>
      <th>Id</th>
      <th>Title</th>
      <th>Text</th>
      <th>Created_at</th>
      <th>Updated_at</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
      <tr>
        <td><a href="{{ path('post_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
        <td>{{ entity.title }}</td>
        <td>{{ entity.text }}</td>
        <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
        <td>{% if entity.updatedat %}{{ entity.updatedat|date('Y-m-d H:i:s') }}{% endif %}</td>
        <td>
          <div class="btn-group">
              <a class="btn btn-default" href="{{ path('post_show', { 'id': entity.id }) }}">show</a>
              <a class="btn btn-default" href="{{ path('post_edit', { 'id': entity.id }) }}">edit</a>
          </div>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <ul>
    <li>
      <a class="btn btn-default" href="{{ path('post_new') }}">
        Create a new entry
      </a>
    </li>
  </ul>
{% endblock %}
